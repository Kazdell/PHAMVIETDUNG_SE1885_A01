@model IEnumerable<PHAMVIETDUNG_SE1885_A01_FE.Presentation.ViewModels.NewsArticleViewModel>

@{
    ViewData["Title"] = "My News History";
}

<div class="d-flex flex-column flex-md-row justify-content-between align-items-start align-items-md-center mb-4 gap-3 animate__animated animate__fadeInDown">
    <div>
        <h1 class="h3 fw-bold mb-1 text-dark">
            <i class="bi bi-clock-history text-primary me-2"></i>My News History
        </h1>
        <p class="text-muted mb-0">Track and manage your published articles.</p>
    </div>
</div>

<div class="card shadow-lg border-0 rounded-4 overflow-hidden animate__animated animate__fadeInUp">
    <div id="newsTableContainer" class="card-body p-0">
        <partial name="_NewsTablePartial" model="Model" />
    </div>
</div>

<!-- Modal Container (Shared) -->
<div class="modal fade" id="actionModal" tabindex="-1" aria-labelledby="actionModalLabel" aria-hidden="true">
    <div id="modal-placeholder"></div>
</div>

@section Scripts {
    <script>
        // Override pagination function to work with standard Controller Action "History"
        function filterNews(page) {
            // Add loading effect
             $('#newsTableContainer').css('opacity', '0.6');
             window.location.href = '@Url.Action("History", "Staff")?page=' + page;
        }
    </script>
}
